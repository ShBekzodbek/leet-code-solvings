

function maxUncrossedLines(nums1,nums2){
    const newArray = [];
    let counter = 0;
    for(let key of nums1){
        if(nums1[key].includes(nums1)){
        
         for(let key of nums2){
            if(nums2[key].includes(nums2)){

            }

         }
        }
    }
}